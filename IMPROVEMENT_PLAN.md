# pfa-nav 改进计划

> 2026-06-02 · 优化根目录的导航脚本 · 高优先级批次

| #  | 任务                     | 状态 |
|----|--------------------------|------|
| H1 | 精简仓库、删除冗余脚本   | 待办 |
| H2 | 提升导航脚本可读性       | 待办 |
| H3 | 提前备好红蓝两套脚本     | 待办 |
| H4 | 加启动保护、跑通所有脚本 | 待办 |

## H1 精简仓库、删除冗余脚本

根目录有十几个脚本，有些可能是旧版本或已被取代（例如 nav.py 之于功能更全的 auto_nav.py，hp_nav.py 与 hp_gimbal_nav.py，cmd_to_gimbal.py 与 cmd_vel_to_gimbal.py 看名字像是重复的）。先逐个确认哪些还在用，把废弃的删掉；剩下的脚本里那些一模一样的代码（读航点、失败重试）再合并到一个公共文件，不再到处抄。

- [ ] 逐个确认根目录脚本是否还在用，废弃的删除
- [ ] 保留的脚本不再各自重复"读航点"等代码
- [ ] 精简后导航功能照常

## H2 提升导航脚本可读性

让脚本更好读、以后加新逻辑时更顺手。比如判断"是否到达"现在 nav.py、game.py 直接写数字 `4`（auto_nav.py 已经用带名字的写法），统一成看得懂的写法；过长的函数拆短、命名更直观。

- [ ] 不再用数字 `4` 这类看不懂的写法
- [ ] 加新目标点 / 新逻辑时不用大改结构

## H3 提前备好红蓝两套脚本

比赛分红方 / 蓝方，两边话题名字、航点文件都不同。现在要临时进代码里改（README 就写着"记得改 game.py"），赛场紧张容易忘。改动后：赛前就把红、蓝各自的脚本 / 启动方式都准备好，到场按颜色直接选对应的跑，不用临时改。

- [ ] 红、蓝各有一套现成、能直接跑的脚本
- [ ] 赛场切换不用改任何代码，两套都实测过

## H4 加启动保护、跑通所有脚本

两件事：① 导航系统（Nav2）没启动时，脚本现在会一直死等、没有提示；改成等不到就超时报错，方便现场发现问题。② 改完逐个脚本实测，确认都能正常启动运行。（顺带检查 game.py 里串口和导航两个线程同时改变量可能出错的隐患。）

- [ ] 导航系统没开时，脚本超时报错、不再傻等
- [ ] 要用的脚本都实测跑通一遍

## 后续批次（待规划）

- M1 给纯计算函数补测试，调参时不怕改坏
- M2 写一份"每个脚本干嘛的、什么时候用"的说明
- L1 清理没用的代码（调试残留、注释掉的旧代码、空函数）
- L2 把散落的脚本归拢成一个目录 / 包
